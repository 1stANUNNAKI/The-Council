# تقرير تحديث: إضافة درج شبكة الأوامر التفاعلية وتوسيع معالج أوامر الطرفية الحية

**التاريخ والوقت الفعلي:** 2026-09-14T13:48:00+03:00  
**المشروع:** المجلس — MAJLIS · The Council  
**الوكيل المسؤول:** 💻 صانع الأكواد (Agent 15) & 🧼 منسق الأكواد النظيفة (Agent 22)  

---

## 1. ملخص التعديل
تم حل مشكلة ظهور "أمر غير معروف" في الطرفية التفاعلية الحية (`Section 03 - #demo`) عبر توفير بيئة استعراض تفاعلية شاملة:
1. إضافة زر بارز أسفل أزرار التجربة (`#cmdToggleBtn`) يفتح درجاً منسدلاً يحتوي على **شبكة بطاقات لكافة الأوامر الـ 12 المدعومة**.
2. تزويد كل بطاقة أمر بأيقونة نسخ سريعة للحافظة مع إشعار مرئي، وزر تشغيل مباشر، وعرض الوكيل والمهارة المسندين ووصف وظيفي دقيق.
3. ترقية محرك الطرفية (`runInput` و `runCmd`) ليتعرف على الأوامر الإنجليزية والبادئات (`/majlis:`) والأسماء العربية (مثل `ابدأ`، `خطط`، `ابن`، `أمن`، `مراجعة`، `اختبار`، `فحص`، `مهارات`، `حالة`، `وكلاء`، `مساعدة`).
4. تزويد استجابة "الأمر غير معروف" بدليل إرشادي يوجه المستخدم فوراً للأوامر الصحيحة واستخدام شبكة البطاقات.

---

## 2. قائمة الأوامر الـ 12 المدعومة في المحاكاة
1. **`start` / `/majlis:start` / `ابدأ`:** إجلاس المجلس الـ40 وتفعيل بوابات الفيتو (`@hadi-core` & `@hadi-maestro`).
2. **`plan` / `/majlis:plan` / `خطط`:** استكشاف الكود وتفكيك المتطلبات لموجات بعقود تحقق (`@product-shaper` & `@integrative-architect`).
3. **`build` / `/majlis:build` / `ابن`:** تنفيذ موجات البناء المتوازية وتوليد commits ذرية (`@emad-api-shield` & `@motion-coder`).
4. **`security` / `/majlis:security` / `أمن`:** فتح البوابة الحمراء وفحص TruffleHog و Nuclei و ZAP (`@sareem-security`).
5. **`review` / `/majlis:review` / `مراجعة`:** بوابة نظافة الكود وفرض معايير SOLID وقاعدة الـ 20 سطراً (`@nadif-clean-code`).
6. **`qa` / `test` / `اختبار`:** تشغيل أجنحة الاختبارات الآلية الشاملة (1731 فحصاً) (`@baher-qa`).
7. **`audit` / `scan` / `فحص`:** الفحص التقني 360 درجة وتحليل اختناقات الأداء ونطاق الانفجار (`@risk-assessor`).
8. **`skills` / `tools` / `مهارات`:** استعراض فهرس المهارات الإنتاجية الـ 90 (`@mubtakir-tools`).
9. **`status` / `/majlis:status` / `حالة`:** لوحة جاهزية المجلس ومحرك الإقلاع والشفاء الذاتي (`@hadi-core`).
10. **`agents` / `وكلاء`:** استعراض مقاعد الأربعين وكيلاً بتفاصيلهم وأقسامهم (`@agent-weaver`).
11. **`help` / `مساعدة`:** دليل المساعدة واستعراض الأوامر السريعة (`@balegh-docs`).
12. **`clear` / `مسح`:** مسح شاشة الطرفية والبدء من جديد.

---

## 3. نتائج التحقق (Verification Gates)
- **جناح التحقق (`node eval/run.js`):**
  - النتيجة: **1731 PASS · 0 FAIL** (100% اجتياز).
- **الفحص البصري التفاعلي:**
  - التحقق من انسيابية فتح وغلق الدرج، ونسخ الأوامر، وتشغيل أمر `review` حياً وتحديث حالة الأزرار.
