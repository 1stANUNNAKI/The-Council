# تقرير تحديث: إصلاح دوران العبارات العربية وتوسيع الطية الأولى بالكامل

- **التاريخ والوقت الفعلي:** 2026-09-14T14:07:30+03:00
- **المشروع:** المجلس — The Council
- **الملفات المعدلة:** `index.html`
- **حالة الاختبارات:** 1731 PASS · 0 FAIL (100% بنجاح)

---

### 1. ملخص التعديل
معالجة مشكلتين رئيسيتين في الواجهة الرئيسية للموقع (`index.html`):
1. **عطل ثبات العبارة في اللغة العربية:** عند التحويل إلى اللغة العربية، كانت العبارات متوقفة على عبارة ثابتة "أعطِه رئيساً." دون دوران، بينما تدور بالإنجليزية.
2. **توسيع قسم الهيرو وتأطير الطية الأولى (Full Viewport Fold):** جعل قسم البداية يمتد على كامل ارتفاع الشاشة الأولى (`min-height: calc(100vh - 84px)`) بحيث يتموضع العنوان والوسام في المنطقة العليا، وتتموضع بطاقات العمل ("شاهده يعمل"، "نجّمه على GitHub"، "محاكاة عمل المجلس") والنص التوضيحي الثانوي وشريط التثبيت في ذيل الطية الأولى.

---

### 2. التشخيص الفني (Root Cause Analysis)
1. **السبب الجذري لثبات العبارة:**
   - كانت دالة `render()` تفحص `if(cur==='en')` لتشغيل `spinWord()` وحقن عنصر `<span class="kick2 rotline" id="rot"></span>`.
   - في فرع `else` (اللغة العربية)، كانت الدالة تحقن `d.h1` الثابت الحاوي على معرّف `id="rota"` دون وجود `id="rot"`، ولم تكن دالة `spinWord()` تُستدعى مطلقاً في الوضع العربي.
   - كانت مصفوفة الكلمات العربية `L.ar.rot` متوفرة بـ 5 عبارات بلاغية (`رئيساً.`، `مراجعاً.`، `عينين ثانيتين.`، `بوابة حمراء.`، `دليلاً جارياً.`) لكنها مهملة التنفيذ.
2. **السبب الجذري لضيق القسم:**
   - وسم `.hero` كان يعتمد على `padding` عمودي نسبي فقط بدون أي حد أدنى للارتفاع (`min-height`)، مما جعل الطية الأولى مكدسة دون استغلال المساحة البصرية الكاملة للشاشة.

---

### 3. الإجراءات والحلول المنفذة
1. **توحيد محرك دوران الكلمات (`spinWord`):**
   - تعديل دالة `render()` لحقن عنصر الدوران `<span class="kick2 rotline" id="rot"></span>` في كلتا اللغتين العربية والإنجليزية.
   - استدعاء `spinWord()` فورياً وتحديث النص المبدئي دون تأخير مع تفعيل انتقال حركي ناعم ثنائي الأبعاد (`opacity` و `transform: translateY`).
   - ربط مصفوفة الدوران التلقائي بـ `t().rot` لتتغير بسلاسة كل 2.3 ثانية في كلا الوضعين.
2. **هيكلة قسم الهيرو وتوسيع الطية الأولى:**
   - ضبط `.hero` ليكون بنية مرنة `display: flex; flex-direction: column; justify-content: space-between; min-height: calc(100vh - 84px);`.
   - تقسيم المحتوى داخلياً إلى حاويتين:
     - `.hero-top`: يضم وسام البداية `#kick` والعنوان الرئيسي الضخم `#h1`.
     - `.hero-bottom`: يضم النص الثانوي `#tag`، أزرار العمل `#cta`، شريط التثبيت السريع `#install`، وصناديق الإحصائيات `#stats`.
   - هذا التوزيع يضمن تموضع الأزرار والنص الثانوي في أسفل وذيل الطية الأولى مباشرةً دون الحاجة للتمرير، مع المحافظة على التوافق التام مع الجوال والشاشات العريضة.

---

### 4. نتائج التحقق والاختبار (Verification)
- **الاختبار الآلي البرمجي:** تشغيل `node eval/run.js` بنتيجة `1731 PASS · 0 FAIL`.
- **الفحص البصري التفاعلي (Browser Subagent):**
  - التحقق من دوران الكلمات العربية بالترتيب: ("عينين ثانيتين." ← "رئيساً." ← "دليلاً جارياً.").
  - التحقق من اتساع الطية الأولى وتموضع الأزرار في ذيل الشاشة الأولى بدقة وجمالية.
